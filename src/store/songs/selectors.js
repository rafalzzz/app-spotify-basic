export const songsCount = state => state.songs.songs.resultCount;

export const songsArray = state => state.songs.songs.results;

export const loadingSongs = state => state.songs.songsLoading;

export const isError = state => state.songs.isError;
